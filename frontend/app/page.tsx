{`import LoginCard from "../components/LoginCard";

export default function Home() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: "#F5F7FA",
        padding: "20px"
      }}
    >
      <LoginCard />
    </main>
  );
}`}
