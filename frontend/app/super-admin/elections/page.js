"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import DashboardShell from "../../../components/dashboard/DashboardShell";
import superAdminNavigation from "../../../components/dashboard/superAdminNavigation";
import ElectionDeploymentSelector from "../../../components/dashboard/ElectionDeploymentSelector";

const API_URL = (process.env.NEXT_PUBLIC_API_URL || "https://polisync-platform-1.onrender.com").replace(/\/+$/, "");
const token = () => typeof window === "undefined" ? "" : ["polisync_token", "authToken", "accessToken", "token"].map((k) => localStorage.getItem(k) || sessionStorage.getItem(k)).find(Boolean) || "";
const pad = (n) => String(n).padStart(2, "0");
const today = new Date();
const todayDate = `${today.getFullYear()}-${pad(today.getMonth() + 1)}-${pad(today.getDate())}`;
const EMPTY = { name: "", electionDate: todayDate, startTime: "08:00", endTime: "17:00", type: "Presidential", country: "Ghana", status: "Draft", totalPollingStations: 0, parties: [], candidates: [] };

function authHeaders(json = true) { const t = token(); return { ...(json ? { "Content-Type": "application/json" } : {}), Accept: "application/json", ...(t ? { Authorization: `Bearer ${t}` } : {}) }; }
async function request(path, options = {}) { const response = await fetch(`${API_URL}${path}`, { cache: "no-store", ...options, headers: { ...authHeaders(!(options.body instanceof FormData)), ...(options.headers || {}) } }); const body = await response.json().catch(() => ({})); if (!response.ok || body.success === false) throw new Error(body.message || `Request failed (${response.status})`); return body; }
function dateTime(date, time) { return date && time ? `${date}T${time}:00+00:00` : null; }
function dateLabel(value) { const d = new Date(value); return Number.isNaN(d.getTime()) ? "Date not set" : new Intl.DateTimeFormat("en-GH", { year: "numeric", month: "long", day: "numeric", timeZone: "Africa/Accra" }).format(d); }
function timeLabel(value) { const d = new Date(value); return Number.isNaN(d.getTime()) ? "" : new Intl.DateTimeFormat("en-GH", { hour: "numeric", minute: "2-digit", hour12: true, timeZone: "Africa/Accra" }).format(d); }
function fileToDataUrl(file) { return new Promise((resolve, reject) => { if (!file) return resolve(""); if (!file.type.startsWith("image/")) return reject(new Error("Please select an image file.")); if (file.size > 2 * 1024 * 1024) return reject(new Error("Party logos must be 2 MB or smaller.")); const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = () => reject(new Error("Unable to read the logo.")); reader.readAsDataURL(file); }); }
function formFromElection(e) { const start = e.startDateTime ? new Date(e.startDateTime) : null; const end = e.endDateTime ? new Date(e.endDateTime) : null; const localDate = start && !Number.isNaN(start.getTime()) ? new Intl.DateTimeFormat("en-CA", { year: "numeric", month: "2-digit", day: "2-digit", timeZone: "Africa/Accra" }).format(start) : `${e.year || today.getFullYear()}-01-01`; const localTime = (d, fallback) => d && !Number.isNaN(d.getTime()) ? new Intl.DateTimeFormat("en-GH", { hour: "2-digit", minute: "2-digit", hour12: false, timeZone: "Africa/Accra" }).format(d) : fallback; return { name: e.name || "", electionDate: localDate, startTime: localTime(start, "08:00"), endTime: localTime(end, "17:00"), type: e.type || "Presidential", country: e.country || "Ghana", status: e.status || "Draft", totalPollingStations: e.totalPollingStations || 0, parties: Array.isArray(e.parties) ? e.parties : [], candidates: Array.isArray(e.candidates) ? e.candidates : [] }; }

export default function ElectionsPage() {
  const [elections, setElections] = useState([]), [parties, setParties] = useState([]), [form, setForm] = useState(EMPTY), [editing, setEditing] = useState(null), [view, setView] = useState("all"), [loading, setLoading] = useState(true), [saving, setSaving] = useState(false), [logoSaving, setLogoSaving] = useState(null), [error, setError] = useState(""), [notice, setNotice] = useState("");

  const load = useCallback(async () => { setLoading(true); setError(""); try { const [e, p] = await Promise.all([request("/api/elections"), request("/api/elections/parties")]); setElections(Array.isArray(e.elections) ? e.elections : []); setParties(Array.isArray(p.parties) ? p.parties : []); } catch (err) { setError(err.message); } finally { setLoading(false); } }, []);
  useEffect(() => { load(); }, [load]);
  const visible = useMemo(() => view === "live" ? elections.filter((e) => e.status === "Active") : view === "history" ? elections.filter((e) => e.status === "Closed") : elections, [elections, view]);
  const setField = (key, value) => setForm((current) => ({ ...current, [key]: value }));
  const selectedIds = new Set((form.parties || []).map((p) => String(p.partyId || "")));

  const addSystemParty = (event) => { const id = event.target.value; event.target.value = ""; if (!id || selectedIds.has(String(id))) return; const party = parties.find((p) => String(p.id) === String(id)); if (!party) return; setField("parties", [...form.parties, { partyId: party.id, name: party.name, logoUrl: party.logoUrl || "" }]); };
  const removeParty = (id) => { setField("parties", form.parties.filter((p) => String(p.partyId) !== String(id))); setField("candidates", form.candidates.map((c) => c.party && String(c.partyId) === String(id) ? { ...c, partyId: null, party: "", partyLogoUrl: "" } : c)); };
  const updateCandidate = (i, key, value) => setField("candidates", form.candidates.map((c, n) => n === i ? { ...c, [key]: value } : c));
  const addCandidate = () => setField("candidates", [...form.candidates, { name: "", partyId: null, party: "", partyLogoUrl: "", profilePictureUrl: "", constituencyId: null }]);
  const chooseCandidateParty = (i, id) => { const party = parties.find((p) => String(p.id) === String(id)); updateCandidate(i, "partyId", party?.id || null); updateCandidate(i, "party", party?.name || "Independent"); updateCandidate(i, "partyLogoUrl", party?.logoUrl || ""); };

  const updatePartyLogo = async (partyId, file) => { if (!file) return; try { setLogoSaving(String(partyId)); const logoUrl = await fileToDataUrl(file); const result = await request(`/api/elections/parties/${partyId}/logo`, { method: "PATCH", body: JSON.stringify({ logoUrl }) }); setParties((current) => current.map((p) => String(p.id) === String(partyId) ? { ...p, logoUrl: result.party.logoUrl } : p)); setForm((current) => ({ ...current, parties: current.parties.map((p) => String(p.partyId) === String(partyId) ? { ...p, logoUrl: result.party.logoUrl } : p), candidates: current.candidates.map((c) => String(c.partyId) === String(partyId) ? { ...c, partyLogoUrl: result.party.logoUrl } : c) })); setNotice("Political party logo updated in the system and this election."); } catch (err) { setError(err.message); } finally { setLogoSaving(null); } };

  const save = async (event) => { event.preventDefault(); setSaving(true); setError(""); setNotice(""); try { if (!form.name.trim()) throw new Error("Election name is required."); if (!form.electionDate || !form.startTime || !form.endTime) throw new Error("Select the election date and voting times."); const startDateTime = dateTime(form.electionDate, form.startTime); const endDateTime = dateTime(form.electionDate, form.endTime); if (new Date(endDateTime) <= new Date(startDateTime)) throw new Error("End time must be after start time."); const payload = { ...form, year: Number(form.electionDate.slice(0, 4)), startDateTime, endDateTime, totalPollingStations: Number(form.totalPollingStations || 0), parties: form.parties.map((p) => ({ partyId: p.partyId, name: p.name, logoUrl: p.logoUrl || "" })), candidates: form.candidates.map((c) => ({ name: c.name, party: c.party || "Independent", partyLogoUrl: c.partyLogoUrl || "", profilePictureUrl: c.profilePictureUrl || "", constituencyId: c.constituencyId || null })) }; delete payload.electionDate; delete payload.startTime; delete payload.endTime; const result = await request(editing ? `/api/elections/${editing._id}` : "/api/elections/create", { method: editing ? "PATCH" : "POST", body: JSON.stringify(payload) }); setNotice(editing ? "Election updated successfully." : "Election created successfully."); setEditing(null); setForm(EMPTY); setElections((current) => editing ? current.map((e) => e._id === result.election._id ? result.election : e) : [result.election, ...current]); } catch (err) { setError(err.message); } finally { setSaving(false); } };
  const editElection = (e) => { setEditing(e); setForm(formFromElection(e)); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const deleteElection = async (e) => { if (!window.confirm(`Delete “${e.name}”? This cannot be undone.`)) return; try { await request(`/api/elections/${e._id}`, { method: "DELETE" }); setElections((current) => current.filter((x) => x._id !== e._id)); setNotice("Election deleted successfully."); } catch (err) { setError(err.message); } };

  return <DashboardShell role="super_admin" navigation={superAdminNavigation} activeSection="elections" title="Election Management" subtitle="Create and control elections using PoliSync system data">
    <main className="page">
      <section className="hero"><div><span>POLISYNC AFRICA • ELECTION CONTROL</span><h1>Election Management</h1><p>Participating parties must come from the registered political parties in the PoliSync system. Super Admin can maintain their official party logos here.</p></div><button onClick={load} className="refresh">↻ Refresh</button></section>
      {notice && <div className="notice success">✓ {notice}</div>}{error && <div className="notice error">{error}</div>}
      <section className="composer">
        <div className="section-title"><div><h2>{editing ? "Edit Election" : "Create Election"}</h2><p>Election date and time are saved in Ghana time (GMT / Africa-Accra).</p></div>{editing && <button type="button" className="cancel" onClick={() => { setEditing(null); setForm(EMPTY); }}>Cancel</button>}</div>
        <form onSubmit={save} className="form-grid">
          <label className="wide">Election name<input required value={form.name} onChange={(e) => setField("name", e.target.value)} placeholder="Ghana General Election" /></label>
          <label>Election date<input required type="date" value={form.electionDate} onChange={(e) => setField("electionDate", e.target.value)} /></label>
          <label>Start time<input required type="time" value={form.startTime} onChange={(e) => setField("startTime", e.target.value)} /></label>
          <label>End time<input required type="time" value={form.endTime} onChange={(e) => setField("endTime", e.target.value)} /></label>
          <label>Election type<select value={form.type} onChange={(e) => setField("type", e.target.value)}><option>Presidential</option><option>Parliamentary</option><option>Local</option></select></label>
          <label>Country<input value={form.country} onChange={(e) => setField("country", e.target.value)} /></label>
          <label>Status<select value={form.status} onChange={(e) => setField("status", e.target.value)}><option>Draft</option><option>Active</option><option>Closed</option></select></label>
          <label>Total polling stations<input type="number" min="0" value={form.totalPollingStations} onChange={(e) => setField("totalPollingStations", e.target.value)} /></label>
          <div className="schedule"><strong>Voting window</strong><span>{form.electionDate ? dateLabel(`${form.electionDate}T00:00:00+00:00`) : "Select date"} · {form.startTime} – {form.endTime} GMT</span></div>
          <div className="submit"><button className="primary" disabled={saving}>{saving ? "Saving…" : editing ? "Save Election Changes" : "Create Election"}</button></div>
        </form>

        <section className="party-section"><div className="section-title"><div><h3>Participating Political Parties</h3><p>Only approved political parties registered in PoliSync can be selected. No free-text party names are accepted.</p></div><select className="party-picker" defaultValue="" onChange={addSystemParty}><option value="">+ Add system political party</option>{parties.filter((p) => !selectedIds.has(String(p.id))).map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select></div>
          {!parties.length && <div className="empty-party">No approved political parties are currently available in the system.</div>}
          <div className="party-grid">{form.parties.map((party) => <article className="party-card" key={party.partyId}><div className="party-logo">{party.logoUrl ? <img src={party.logoUrl} alt={`${party.name} logo`} /> : <span>LOGO</span>}</div><div className="party-info"><strong>{party.name}</strong><small>System political party</small><label className="logo-upload">{logoSaving === String(party.partyId) ? "Updating logo…" : "Update party logo"}<input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml" disabled={logoSaving !== null} onChange={(e) => updatePartyLogo(party.partyId, e.target.files?.[0])} /></label></div><button type="button" className="remove" onClick={() => removeParty(party.partyId)}>Remove from election</button></article>)}</div>
        </section>

        <section className="party-section"><div className="section-title"><div><h3>Candidates</h3><p>Associate candidates with a selected system party or Independent.</p></div><button type="button" className="add" onClick={addCandidate}>+ Add Candidate</button></div>{form.candidates.map((candidate, i) => <div className="candidate" key={i}><input placeholder="Candidate name" value={candidate.name} onChange={(e) => updateCandidate(i, "name", e.target.value)} /><select value={candidate.partyId || ""} onChange={(e) => chooseCandidateParty(i, e.target.value)}><option value="">Independent</option>{form.parties.map((p) => <option key={p.partyId} value={p.partyId}>{p.name}</option>)}</select><input placeholder="Profile picture URL (optional)" value={candidate.profilePictureUrl} onChange={(e) => updateCandidate(i, "profilePictureUrl", e.target.value)} /><button type="button" className="remove" onClick={() => setField("candidates", form.candidates.filter((_, n) => n !== i))}>Remove</button></div>)}</section>
      </section>

      <section className="toolbar"><div className="tabs"><button className={view === "all" ? "active" : ""} onClick={() => setView("all")}>All Elections <b>{elections.length}</b></button><button className={view === "live" ? "active" : ""} onClick={() => setView("live")}>Live <b>{elections.filter((e) => e.status === "Active").length}</b></button><button className={view === "history" ? "active" : ""} onClick={() => setView("history")}>History <b>{elections.filter((e) => e.status === "Closed").length}</b></button></div></section>
      <section className="list">{loading ? <div className="empty">Loading elections…</div> : visible.length === 0 ? <div className="empty">No elections in this view.</div> : visible.map((e) => <article className="election" key={e._id}><div className="identity"><span className={`badge ${String(e.status || "").toLowerCase()}`}>{e.status}</span><h3>{e.name}</h3><p>{e.country} • {e.type} • {dateLabel(e.startDateTime || `${e.year}-01-01T00:00:00Z`)}</p>{e.startDateTime && e.endDateTime && <div className="window">{timeLabel(e.startDateTime)} – {timeLabel(e.endDateTime)} GMT</div>}<div className="party-logos">{(e.parties || []).map((p, i) => <span key={i} title={p.name}>{p.logoUrl ? <img src={p.logoUrl} alt="" /> : p.name.slice(0, 2).toUpperCase()}</span>)}</div></div><div className="metrics"><div><small>Polling stations</small><strong>{Number(e.totalPollingStations || 0).toLocaleString()}</strong></div><div><small>Parties</small><strong>{(e.parties || []).length}</strong></div><div><small>Candidates</small><strong>{(e.candidates || []).length}</strong></div></div><div className="actions"><button onClick={() => editElection(e)}>Edit</button>{e.status !== "Active" && <button className="delete" onClick={() => deleteElection(e)}>Delete</button>}</div></article>)}</section>
      {editing && <ElectionDeploymentSelector />}
    </main>
    <style jsx>{styles}</style>
  </DashboardShell>;
}

const styles = `.page{min-height:100%;padding:clamp(14px,3vw,38px);background:#f5f8f6;color:#183d2e;box-sizing:border-box}.hero,.composer,.toolbar,.list,.notice{max-width:1200px;margin-left:auto;margin-right:auto}.hero{display:flex;justify-content:space-between;gap:20px;align-items:flex-end;margin-bottom:18px}.hero span{font-size:10px;font-weight:900;letter-spacing:1.8px;color:#bd941d}.hero h1{margin:7px 0;color:#075d2e;font-size:clamp(30px,5vw,50px)}.hero p{margin:0;color:#708078;max-width:800px;line-height:1.55}.refresh,.primary,.add{border:0;border-radius:10px;background:#075d2e;color:#fff;padding:11px 15px;font-weight:850;cursor:pointer}.composer{background:#fff;border:1px solid #dce6e0;border-radius:18px;padding:22px;box-shadow:0 8px 25px rgba(20,60,42,.06)}.section-title{display:flex;justify-content:space-between;align-items:center;gap:16px}.section-title h2,.section-title h3{margin:0;color:#075d2e}.section-title p{margin:4px 0 0;color:#77857e;font-size:12px;line-height:1.5}.cancel,.remove,.party-picker{border:1px solid #cedbd3;background:#fff;color:#234c39;border-radius:9px;padding:9px 12px;font-weight:800}.form-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:13px;margin-top:20px}.form-grid label{display:grid;gap:6px;font-size:11px;font-weight:850;color:#385746}.wide{grid-column:1/-1}.form-grid input,.form-grid select,.candidate input,.candidate select{width:100%;box-sizing:border-box;border:1px solid #d6e1db;border-radius:9px;background:#fbfdfc;color:#17392b;padding:11px 12px;font:inherit;font-size:16px;outline:none}.form-grid input:focus,.form-grid select:focus,.candidate input:focus,.candidate select:focus{border-color:#08713a;box-shadow:0 0 0 3px rgba(8,113,58,.08)}.schedule{grid-column:1/3;border:1px solid #dbe7df;border-radius:11px;padding:11px 13px;background:#f4f9f6;display:grid;gap:3px}.schedule strong{font-size:10px;color:#075d2e;text-transform:uppercase;letter-spacing:1px}.schedule span{font-size:12px;color:#5d7066}.submit{grid-column:3/-1;display:flex;align-items:end;justify-content:flex-end}.primary{min-height:44px}.primary:disabled{opacity:.6}.party-section{margin-top:24px;padding-top:20px;border-top:1px solid #e5ece8}.party-picker{min-width:230px}.empty-party,.empty{padding:28px;text-align:center;color:#718078;border:1px dashed #ccd9d2;border-radius:12px;margin-top:12px}.party-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-top:13px}.party-card{display:flex;align-items:center;gap:12px;border:1px solid #dbe6df;border-radius:12px;padding:11px;background:#fbfdfc;min-width:0}.party-logo{width:58px;height:58px;flex:0 0 58px;border-radius:10px;background:#eef4f0;display:grid;place-items:center;overflow:hidden;color:#7a8b82;font-size:9px;font-weight:900}.party-logo img{width:100%;height:100%;object-fit:contain}.party-info{min-width:0;display:grid;gap:3px;flex:1}.party-info strong{font-size:14px;overflow-wrap:anywhere}.party-info small{font-size:9px;color:#789085}.logo-upload{position:relative;width:max-content;max-width:100%;padding:6px 8px;border:1px solid #cbd9d1;border-radius:7px;background:#fff;color:#17613c;font-size:9px;font-weight:900;cursor:pointer}.logo-upload input{position:absolute;inset:0;width:100%;height:100%;opacity:0;cursor:pointer}.add{padding:9px 12px}.candidate{display:grid;grid-template-columns:1.3fr 1fr 1.3fr auto;gap:8px;margin-top:9px}.toolbar{margin-top:18px}.tabs{display:flex;gap:7px;overflow:auto}.tabs button{border:1px solid #d5e0da;background:#fff;color:#52675c;padding:9px 12px;border-radius:9px;font-weight:800;white-space:nowrap;cursor:pointer}.tabs button.active{background:#075d2e;color:#fff;border-color:#075d2e}.tabs b{margin-left:5px}.list{display:grid;gap:10px;margin-top:12px}.election{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:20px;align-items:center;background:#fff;border:1px solid #dce6e0;border-radius:14px;padding:15px;box-shadow:0 5px 15px rgba(20,60,42,.04)}.identity{min-width:0}.badge{display:inline-block;padding:4px 7px;border-radius:999px;background:#edf1ef;color:#64736b;font-size:8px;font-weight:900;text-transform:uppercase}.badge.active{background:#e3f5e9;color:#08713a}.badge.draft{background:#fff6db;color:#866713}.badge.closed{background:#f0eeee;color:#7c5555}.identity h3{margin:7px 0 3px;color:#174e35;font-size:17px;overflow-wrap:anywhere}.identity p{margin:0;color:#748078;font-size:11px}.window{margin-top:5px;font-size:10px;font-weight:800;color:#075d2e}.party-logos{display:flex;gap:4px;margin-top:9px}.party-logos span{width:25px;height:25px;border-radius:50%;background:#edf5ef;display:grid;place-items:center;overflow:hidden;font-size:7px;font-weight:900;color:#31614a}.party-logos img{width:100%;height:100%;object-fit:contain}.metrics{display:flex;gap:16px}.metrics div{display:grid;gap:2px;min-width:75px}.metrics small{font-size:9px;color:#849189}.metrics strong{font-size:14px;color:#174e35}.actions{display:flex;gap:6px}.actions button{border:1px solid #ccd9d1;background:#fff;border-radius:8px;padding:8px 10px;color:#23513c;font-weight:800;cursor:pointer}.actions .delete{color:#a33b3b}.notice{padding:11px 13px;border-radius:10px;margin-bottom:12px;font-size:12px;font-weight:800}.notice.success{background:#e9f7ee;color:#17623d}.notice.error{background:#fff0f0;color:#9d3434}
@media(max-width:900px){.form-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.wide{grid-column:1/-1}.schedule,.submit{grid-column:1/-1}.submit{justify-content:stretch}.submit .primary{width:100%}.party-grid{grid-template-columns:1fr}.election{grid-template-columns:1fr}.metrics{flex-wrap:wrap}.actions{justify-content:flex-start}.hero{align-items:flex-start}}
@media(max-width:620px){.page{padding:12px}.hero{display:block}.refresh{margin-top:12px;width:100%}.composer{padding:14px;border-radius:14px}.section-title{display:block}.section-title .cancel,.party-picker,.add{margin-top:10px;width:100%;box-sizing:border-box}.form-grid{grid-template-columns:1fr}.wide,.schedule,.submit{grid-column:1}.party-card{align-items:flex-start;flex-wrap:wrap}.party-logo{width:52px;height:52px;flex-basis:52px}.remove{margin-left:auto}.candidate{grid-template-columns:1fr}.candidate .remove{width:100%}.tabs{padding-bottom:2px}.election{padding:13px}.metrics{gap:10px}.metrics div{min-width:0;flex:1}.actions button{flex:1}}
`;