"use client";
import { useEffect, useMemo, useState } from "react";

const AttachmentList = ({ message, api, token }) => {
  const [urls, setUrls] = useState({});
  useEffect(() => {
    let active = true;
    const load = async () => {
      const next = {};
      for (const item of message?.attachments || []) {
        try {
          const r = await fetch(`${api}/api/messages/${message._id}/attachment/${item._id}`, { headers: { Authorization: `Bearer ${token()}` } });
          if (!r.ok) continue;
          const blob = await r.blob();
          next[item._id] = URL.createObjectURL(blob);
        } catch {}
      }
      if (active) setUrls(next); else Object.values(next).forEach(URL.revokeObjectURL);
    };
    load();
    return () => { active = false; Object.values(urls).forEach(URL.revokeObjectURL); };
  }, [message?._id]);
  if (!message?.attachments?.length) return null;
  return <div className="attachments">{message.attachments.map((item) => { const url = urls[item._id]; const image = item.mimeType?.startsWith("image/"); return <div className="attachment" key={item._id}>{image && url ? <a href={url} target="_blank" rel="noreferrer"><img src={url} alt={item.originalName}/></a> : <a href={url || "#"} target="_blank" rel="noreferrer" download={item.originalName} className="fileCard"><span>📎</span><div><b>{item.originalName}</b><small>{Math.max(1, Math.round(item.size / 1024))} KB</small></div></a>}</div>; })}</div>;
};

export default function MessagesPanel() {
  const [messages, setMessages] = useState([]), [sent, setSent] = useState([]), [query, setQuery] = useState(""), [users, setUsers] = useState([]), [recipient, setRecipient] = useState(null), [body, setBody] = useState(""), [files, setFiles] = useState([]), [status, setStatus] = useState(""), [forwarding, setForwarding] = useState(null), [forwardQuery, setForwardQuery] = useState(""), [forwardUsers, setForwardUsers] = useState([]), [forwardRecipients, setForwardRecipients] = useState([]), [forwardStatus, setForwardStatus] = useState(""), [forwardBusy, setForwardBusy] = useState(false);
  const api = (process.env.NEXT_PUBLIC_API_URL || "").replace(/\/+$/, "");
  const token = () => typeof window !== "undefined" ? (localStorage.getItem("polisync_token") || sessionStorage.getItem("polisync_token") || "") : "";
  const load = async () => { try { const r = await fetch(`${api}/api/messages/inbox`, { headers: { Authorization: `Bearer ${token()}` } }); const d = await r.json(); if (d.success) { setMessages(d.messages || []); setSent(d.sent || []); } } catch {} };
  useEffect(() => { load(); }, []);
  const searchUsers = async (value, setter) => { if (!value.trim()) return setter([]); try { const r = await fetch(`${api}/api/messages/users?q=${encodeURIComponent(value.trim())}`, { headers: { Authorization: `Bearer ${token()}` } }); const d = await r.json(); setter(d.users || []); } catch { setter([]); } };
  const search = () => searchUsers(query, setUsers);
  const chooseFiles = (event) => { const chosen = Array.from(event.target.files || []).slice(0, 5); const oversized = chosen.find((f) => f.size > 10 * 1024 * 1024); if (oversized) { setStatus(`${oversized.name} is larger than 10 MB.`); return; } setFiles(chosen); setStatus(""); };
  const send = async () => {
    if (!recipient || (!body.trim() && !files.length)) return;
    try {
      const form = new FormData(); form.append("recipientId", recipient.id); form.append("body", body.trim()); files.forEach((file) => form.append("attachments", file));
      const r = await fetch(`${api}/api/messages`, { method: "POST", headers: { Authorization: `Bearer ${token()}` }, body: form }); const d = await r.json();
      if (!r.ok || !d.success) throw new Error(d.message || "Message could not be sent.");
      setBody(""); setFiles([]); setRecipient(null); setQuery(""); setUsers([]); setStatus("Message sent securely."); load();
    } catch (e) { setStatus(e.message); }
  };
  const displayName = (user) => user?.displayName || user?.username || "User";
  const messageText = (message) => String(message?.body || "");
  const openForward = (message) => { setForwarding(message); setForwardQuery(""); setForwardUsers([]); setForwardRecipients([]); setForwardStatus(""); };
  const toggleForwardRecipient = (user) => setForwardRecipients((current) => current.some((x) => String(x.id) === String(user.id)) ? current.filter((x) => String(x.id) !== String(user.id)) : [...current, user]);
  const forward = async () => { if (!forwarding || !forwardRecipients.length || forwardBusy) return; setForwardBusy(true); setForwardStatus(""); try { const r = await fetch(`${api}/api/messages/forward`, { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token()}` }, body: JSON.stringify({ messageId: forwarding._id, recipientIds: forwardRecipients.map((u) => u.id) }) }); const d = await r.json(); if (!r.ok || !d.success) throw new Error(d.message || "Message could not be forwarded."); setForwardStatus(`${d.forwardedCount} recipient${d.forwardedCount === 1 ? "" : "s"} received the forwarded message.`); setForwardRecipients([]); load(); setTimeout(() => setForwarding(null), 1200); } catch (e) { setForwardStatus(e.message); } finally { setForwardBusy(false); } };
  return <section className="messages">
    <header className="messagesHeader"><div><small className="eyebrow">PRIVATE COMMUNICATION</small><h2>Communications</h2><p>Send messages, photos and files across phones, tablets and computers.</p></div><button className="refresh" onClick={load}>↻</button></header>
    <div className="compose">
      <div className="searchRow"><input value={query} onChange={e => setQuery(e.target.value)} onKeyDown={e => e.key === "Enter" && search()} placeholder="Search name or username"/><button onClick={search}>Search</button></div>
      {users.length > 0 && <div className="results">{users.map(u => <button key={u.id} onClick={() => { setRecipient(u); setUsers([]); }}>{displayName(u)} <small>@{u.username}</small></button>)}</div>}
      {recipient && <><div className="recipient">To: <b>{displayName(recipient)}</b><button onClick={() => setRecipient(null)}>×</button></div><textarea value={body} onChange={e => setBody(e.target.value)} placeholder="Write your message…" maxLength={10000}/><div className="composerTools"><label className="attachButton">▧ Files<input type="file" multiple onChange={chooseFiles}/></label><label className="attachButton">▣ Gallery<input type="file" accept="image/*" multiple onChange={chooseFiles}/></label><span>{files.length ? `${files.length} selected` : "Up to 5 files · 10 MB each"}</span></div>{files.length > 0 && <div className="fileQueue">{files.map((file, i) => <div key={`${file.name}-${i}`}><span>{file.type.startsWith("image/") ? "🖼️" : "📎"}</span><b>{file.name}</b><button onClick={() => setFiles(files.filter((_, index) => index !== i))}>×</button></div>)}</div>}<button className="send" onClick={send}>Send message</button></>}
    </div>
    <div className="list"><div className="sectionTitle"><h3>Inbox</h3><span>{messages.length}</span></div>{messages.length ? messages.map(m => <article key={`in-${m._id}`}><div className="messageMeta"><b>{displayName(m.sender)}</b><small>{new Date(m.createdAt).toLocaleString()}</small></div>{m.forwardedFrom && <div className="forwardedLabel">↗ Forwarded message</div>}<p>{messageText(m)}</p><AttachmentList message={m} api={api} token={token}/><button className="messageAction" onClick={() => openForward(m)}>↗ Forward</button></article>) : <p className="empty">No messages yet.</p>}<div className="sectionTitle sentTitle"><h3>Sent</h3><span>{sent.length}</span></div>{sent.length ? sent.map(m => <article key={`out-${m._id}`}><div className="messageMeta"><b>To {displayName(m.recipient)}</b><small>{new Date(m.createdAt).toLocaleString()}</small></div>{m.forwardedFrom && <div className="forwardedLabel">↗ Forwarded message</div>}<p>{messageText(m)}</p><AttachmentList message={m} api={api} token={token}/><button className="messageAction" onClick={() => openForward(m)}>↗ Forward</button></article>) : <p className="empty">No sent messages yet.</p>}</div>
    {status && <div className="statusMessage">{status}</div>}
    {forwarding && <div className="forwardOverlay"><div className="forwardModal"><div className="forwardHeader"><div><small className="eyebrow">MESSAGE ACTION</small><h3>Forward message</h3></div><button className="close" onClick={() => setForwarding(null)}>×</button></div><div className="preview">{messageText(forwarding)}<AttachmentList message={forwarding} api={api} token={token}/></div><input value={forwardQuery} onChange={e => { setForwardQuery(e.target.value); searchUsers(e.target.value, setForwardUsers); }} placeholder="Find one or more people…"/>{forwardUsers.length > 0 && <div className="forwardResults">{forwardUsers.map(u => { const selected = forwardRecipients.some(x => String(x.id) === String(u.id)); return <button className={selected ? "selected" : ""} key={u.id} onClick={() => toggleForwardRecipient(u)}>{displayName(u)} <small>@{u.username}</small>{selected && <b>✓</b>}</button>; })}</div>}{forwardRecipients.length > 0 && <div className="chips">{forwardRecipients.map(u => <button key={u.id} onClick={() => toggleForwardRecipient(u)}>{displayName(u)} ×</button>)}</div>}<div className="forwardFooter"><span>{forwardRecipients.length} selected</span><button className="cancel" onClick={() => setForwarding(null)}>Cancel</button><button className="forwardButton" disabled={!forwardRecipients.length || forwardBusy} onClick={forward}>{forwardBusy ? "Forwarding…" : "Forward message"}</button></div>{forwardStatus && <div className="statusMessage">{forwardStatus}</div>}</div></div>}
    <style jsx>{`.messages{width:100%;box-sizing:border-box;padding:22px;border:1px solid #e3ebe5;border-radius:18px;background:#fff;box-shadow:0 8px 24px rgba(17,65,36,.05);overflow:hidden}.messagesHeader{display:flex;justify-content:space-between;gap:12px}.eyebrow{color:#c9a227;font-size:8px;font-weight:900;letter-spacing:1px}.messages h2{margin:4px 0;color:#075f2b;font-size:20px}.messagesHeader p{margin:3px 0;color:#818a84;font-size:11px}.refresh,.close{width:34px;height:34px;border:1px solid #dce5df;border-radius:9px;background:#fff;cursor:pointer}.compose{position:relative;margin-top:18px;padding-top:16px;border-top:1px solid #edf1ee}.searchRow{display:flex;gap:8px}.compose input,.compose textarea,.forwardModal>input{width:100%;min-width:0;box-sizing:border-box;padding:11px 12px;border:1px solid #dce5df;border-radius:9px;outline:none}.searchRow input{flex:1}.searchRow button,.send,.forwardButton{padding:10px 14px;border:0;border-radius:9px;background:#075f2b;color:#fff;font-weight:700;cursor:pointer}.results{position:absolute;z-index:10;top:66px;left:0;right:78px;max-height:240px;overflow:auto;background:#fff;border:1px solid #dce5df;border-radius:10px;box-shadow:0 10px 24px rgba(0,0,0,.08)}.results button,.forwardResults button{display:flex;align-items:center;gap:7px;width:100%;padding:11px;border:0;border-bottom:1px solid #eef1ef;background:#fff;text-align:left;cursor:pointer}.results small,.forwardResults small{color:#8a938d}.recipient{margin-top:10px;padding:9px 11px;background:#eef8f1;border-radius:9px;font-size:11px}.recipient button{float:right;border:0;background:transparent;font-size:18px}.compose textarea{display:block;min-height:90px;margin-top:9px;resize:vertical;font:inherit}.composerTools{display:flex;align-items:center;gap:8px;margin-top:9px;flex-wrap:wrap;color:#7a847e;font-size:9px}.attachButton{position:relative;display:inline-flex;align-items:center;gap:5px;padding:8px 10px;border:1px solid #cfe0d4;border-radius:9px;background:#f5faf6;color:#075f2b;font-weight:800;cursor:pointer}.attachButton input{position:absolute;inset:0;opacity:0;cursor:pointer}.fileQueue{display:grid;gap:6px;margin-top:8px}.fileQueue>div{display:flex;align-items:center;gap:7px;padding:7px 9px;border:1px solid #e3ebe5;border-radius:8px;background:#f8faf8;font-size:10px}.fileQueue b{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.fileQueue button{margin-left:auto;border:0;background:transparent;cursor:pointer}.compose .send{display:block;margin:9px 0 0 auto}.list{margin-top:22px;border-top:1px solid #edf1ee;padding-top:16px}.sectionTitle{display:flex;align-items:center;gap:7px}.sectionTitle h3{margin:0;color:#344139;font-size:13px}.sectionTitle span{min-width:20px;padding:2px 6px;border-radius:999px;background:#eef8f1;color:#075f2b;text-align:center;font-size:9px}.sentTitle{margin-top:24px}.list article{padding:12px 2px;border-bottom:1px solid #f0f3f1}.messageMeta{display:flex;justify-content:space-between;gap:12px}.messageMeta b{font-size:11px;color:#445149}.messageMeta small{color:#929b95;font-size:8px}.list article p{margin:6px 0;color:#5d6861;font-size:11px;white-space:pre-wrap;overflow-wrap:anywhere}.attachments{display:grid;gap:8px;margin:9px 0}.attachment img{display:block;width:min(360px,100%);max-height:280px;object-fit:cover;border-radius:10px;border:1px solid #dce5df}.fileCard{display:flex;align-items:center;gap:9px;width:min(420px,100%);box-sizing:border-box;padding:10px;border:1px solid #dce5df;border-radius:9px;background:#f8faf8;color:#244035;text-decoration:none}.fileCard div{min-width:0}.fileCard b{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:10px}.fileCard small{display:block;margin-top:3px;color:#89938d;font-size:8px}.forwardedLabel{margin-top:7px;color:#8b7020;font-size:9px;font-weight:700}.messageAction{padding:6px 8px;border:1px solid #dce5df;border-radius:7px;background:#fff;color:#075f2b;font-size:9px;font-weight:700;cursor:pointer}.empty{color:#929b95;font-size:10px}.statusMessage{margin-top:12px;padding:9px 10px;border-radius:8px;background:#eef8f1;color:#267043;font-size:10px}.forwardOverlay{position:fixed;inset:0;z-index:1000;display:flex;align-items:center;justify-content:center;padding:16px;background:rgba(8,35,20,.48);backdrop-filter:blur(4px)}.forwardModal{width:min(560px,100%);max-height:92vh;overflow:auto;box-sizing:border-box;padding:18px;border-radius:16px;background:#fff}.forwardHeader{display:flex;justify-content:space-between;gap:12px}.forwardHeader h3{margin:4px 0 12px;color:#075f2b;font-size:18px}.preview{padding:11px;margin-bottom:12px;border-left:3px solid #c9a227;border-radius:8px;background:#f7f9f7;color:#5d6861;font-size:11px}.forwardResults{margin-top:7px;max-height:220px;overflow:auto;border:1px solid #e3ebe5;border-radius:9px}.forwardResults button.selected{background:#eef8f1}.forwardResults button b{margin-left:auto;color:#075f2b}.chips{display:flex;flex-wrap:wrap;gap:6px;margin-top:9px}.chips button{padding:6px 8px;border:1px solid #cfe0d4;border-radius:999px;background:#eef8f1;color:#075f2b;font-size:9px}.forwardFooter{display:flex;align-items:center;gap:7px;margin-top:15px}.forwardFooter span{margin-right:auto;color:#737d77;font-size:10px}.cancel{padding:10px 12px;border:1px solid #dce5df;border-radius:9px;background:#fff}.forwardButton:disabled{opacity:.5}@media(max-width:640px){.messages{padding:14px}.searchRow{display:grid;grid-template-columns:1fr}.searchRow button,.compose .send{width:100%}.results{top:105px;right:0}.messageMeta{flex-direction:column;gap:3px}.forwardOverlay{padding:8px;align-items:flex-end}.forwardModal{width:100%;max-height:88vh;border-radius:16px 16px 10px 10px;padding:15px}.forwardFooter{flex-wrap:wrap}.forwardFooter span{width:100%;margin-bottom:2px}.cancel,.forwardButton{flex:1}.attachments img{max-height:240px}}@media(pointer:coarse){button,input,textarea{font-size:16px}.messageAction{font-size:12px;padding:8px 10px}}`}</style>
  </section>;
}
